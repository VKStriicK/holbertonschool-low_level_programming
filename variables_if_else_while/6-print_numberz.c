#include <stdio.h>

/**
 * main - Main funciton
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int number = 0;

	for (number = 0; number < 10; number++)
	{
		putchar(number);
	}
	putchar('\n');
	return (0);
}
