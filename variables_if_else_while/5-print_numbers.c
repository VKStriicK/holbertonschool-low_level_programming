#include <stdio.h>

/**
 * main - Main function
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int number = 0;

	for (number = 0 ;  number < 10 ; number++)
	{
		printf("%d", number);
	}
	putchar('\n');
	return (0);
}
