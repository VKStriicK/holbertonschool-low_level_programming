#include "main.h"

/**
 * _strlen - Main function
 * @s: The string to return the length
 *
 * Return: The length of the string s
 */

int _strlen(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		i++
	}

	return (i);
}
